{% extends 'base.html.twig' %}

{% block title %}
	{{ 'security.registration.pageTitle'|trans }}
{% endblock %}

{% block body %}
	<div class="container">
		<h1>{{ 'security.registration.title'|trans }}</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-dismissible alert-success mt-4">
                {{ message }}
            </div>
        {% endfor %}

		{{ form(form) }}
{% endblock %}
