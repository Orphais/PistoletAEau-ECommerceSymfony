<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid flex flex-column">
		<a class="navbar-brand d-flex align-items-center fs-3 fw-bold" href="{{ path('home', {'_locale': app.request.locale}) }}">
			<img src="{{ asset('uploads/ico/ico.png') }}" alt="TirAmisou" height="65" class="me-3">
			TirAmisou
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('home', {'_locale': app.request.locale}) }}">
						<i class="bi bi-house"></i>
						{{ 'navbar.home'|trans }}
					</a>
					<span class="visually-hidden">(current)</span>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('search', {'_locale': app.request.locale}) }}">
						<i class="bi bi-search"></i>
						{{ 'navbar.search'|trans }}
					</a>
				</li>
				{% if app.user %}
					<li class="nav-item" data-controller="cart-badge" data-cart-badge-locale-value="{{ app.request.locale }}">
						<a class="nav-link position-relative" href="{{ path('cart', {'_locale': app.request.locale}) }}">
							<i class="bi bi-cart3"></i>
							{{ 'navbar.cart'|trans }}
							<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none" data-cart-badge-target="count">
								0
							</span>
						</a>
					</li>
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('admin') }}">
								<i class="bi bi-speedometer2"></i>
								{{ 'navbar.dashboard'|trans }}
							</a>
						</li>
					{% endif %}
				{% endif %}
			</ul>
			<ul class="navbar-nav ms-auto">
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
						<i class="bi bi-translate"></i>
					</a>
					<div class="dropdown-menu">
						<a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({'_locale': 'fr'})) }}">{{ 'navbar.language.fr'|trans }}</a>
						<a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({'_locale': 'en'})) }}">{{ 'navbar.language.en'|trans }}</a>
					</div>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('security.logout') }}">
							<i class="bi bi-box-arrow-left"></i>
						</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('security.login') }}">{{ 'navbar.login'|trans }}</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('security.registration') }}">{{ 'navbar.register'|trans }}</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
